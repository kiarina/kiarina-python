---
chat_model_config: "@anthropic"
tool_configs: ["@mini"]
---

Check the diff with `git diff --cached` and add the content to the Unreleased section of CHANGELOG.md.
If necessary, update README.md and documentation under docs/.
Finally, `git add` the changes and commit them.
